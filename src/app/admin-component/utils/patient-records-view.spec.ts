import { PatientRecordsView } from './patient-records-view';

describe('PatientRecordsView', () => {
  it('should create an instance', () => {
    expect(new PatientRecordsView()).toBeTruthy();
  });
});
